"""LSL marker outlet wrapper.

Stream name MUST stay constant (`Chills_Task_Markers`) and source_id MUST
stay constant (`CHILLS_ODDBALL_Launcher`) across the whole session, so
LabRecorder does not lose its subscription mid-session.
"""
